// Google Cloud Credentials for PhD Tracker Desktop App
// These are safe to embed for a Desktop App as long as the Client Secret is treated as "public" 
// (which is standard for installed apps that can't keep secrets).

export const GOOGLE_CLIENT_ID = 'YOUR_GOOGLE_CLIENT_ID';
export const GOOGLE_CLIENT_SECRET = 'YOUR_GOOGLE_CLIENT_SECRET';
